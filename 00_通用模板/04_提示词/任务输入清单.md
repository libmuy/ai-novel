# 任务输入清单（人读视图）

> **派生 · 禁止手工编辑 · 非权威**
> 由 `02_工具/01_小说通用工具/build_prompt_manifest.py --write` 从
> `00_通用模板/04_提示词/任务输入清单.toml`（机读权威）重出。改「某任务要哪些输入、
> 每个输入整份还是取哪几节」请改那份 `.toml`，然后重跑本脚本；直接改本文件会被
> `audit_rules.py` 的 `RULE009` 拦下。

> 「什么任务需要什么信息」此前散在 `00_使用说明.md` 路由表、`00_云端提示词生成器.md`
> 与 `assemble.py` 三处、已经漂移过。现在这三处对脚本化任务（细纲 / 正文）都指向上面那份 `.toml`。

---

## 任务：细纲

- 说明：写单章细纲（规划层产出，不是正文）
- 脚本：`build_prompt.py --task 细纲`（`prompt_build/assemble.py`）
- 骨架段：【你的角色】 → 【必读模板】 → 【已有数据】 → 【任务目标】 → 【输出格式】 → 【验收自检】

| 骨架段 | 输入 | 内联方式 | 区块标题 / 说明 |
|---|---|---|---|
| 【你的角色】 | 本工具撰写 `role_outline` | authored | 角色与纪律 |
| 【必读模板】 | 模板 `02_卡片模板/07_单章细纲模板.md` | 整份 | 单章细纲模板 |
| 【必读模板】 | 模板 `01_写作规则/00_通用写作规则_生成版.md` | 整份 | 通用写作规则（生成版）；细纲阶段只需「怎么写」切片；第八章自检清单属正文阶段 |
| 【必读模板】（opening） | 模板 `01_写作规则/05_开篇三章设计指南.md` | 整份 | 开篇三章设计指南 |
| 【必读模板】 | 计算 `event_templates_beat` | resolver | 按本章节拍「必用模板/核心事件类型」列追加 ≤1 个事件模板 |
| 【已有数据】 | 计算 `beat_block` | resolver | 本卷大纲 · 本章节拍；节拍表本章行——摘要是本章事件归属的唯一权威 |
| 【已有数据】 | 主角档案 | 整份 | 主角档案 |
| 【已有数据】 | 计算 `cast_cards_beat` | resolver | 出场对象 = 节拍表本章摘要里的全部 @引用（下限，细纲须据剧情补齐） |
| 【已有数据】 | 计算 `wr_hard_outline` | resolver | 世界基本法则 · 硬规则清单 |
| 【已有数据】 | 计算 `dy_fh_beat` | resolver | — |
| 【已有数据】 | 计算 `sliding_window` | resolver | 上下文滑动窗口 |
| 【已有数据】 | 计算 `opener_state_outline` | resolver | 本章开篇状态 |
| 【任务目标】 | 本工具撰写 `outline_task` | authored | 产出要求 |
| 【输出格式】 | 本工具撰写 `outline_output_format` | authored | 按模板字段全量输出 |
| 【验收自检】 | 本工具撰写 `outline_selfcheck` | authored | 自检清单 |

## 任务：正文

- 说明：写单章正文
- 脚本：`build_prompt.py --task 正文`（`prompt_build/assemble.py`）
- 骨架段：【你的角色】 → 【必读规则】 → 【已有数据】 → 【任务】 → 【输出格式】 → 【输出后自检】

| 骨架段 | 输入 | 内联方式 | 区块标题 / 说明 |
|---|---|---|---|
| 【你的角色】 | 本工具撰写 `role_manuscript` | authored | 角色与纪律 |
| 【必读规则】（has_redline） | 数据 `01_设定/00_红线包.md` | 整份 | 常驻红线包（本书逐章不变的约束，整段照办）；逐章不变约束的卷级蒸馏视图，打头件 |
| 【必读规则】（no_redline） | 计算 `redline_fallback` | resolver | 红线包未建时退回：生成版 + 文风逐份内联 |
| 【必读规则】 | 数据 `01_设定/00_禁用词表.md` | 整份 | 正文禁用词（完整清单，权威）；红线包 §六 只收高频摘录；云端读不到词表本体，故整份内联 |
| 【必读规则】 | 计算 `sysinst_common` | resolver | 系统指令 · 通用；01_系统指令.md 的「通用指令」+ 核心原则/文风红线/世界法则红线/版权人设红线 |
| 【必读规则】 | 计算 `event_templates_outline` | resolver | 细纲里有【战斗结算要素】/【突破】等区块 → 追加对应事件模板 |
| 【必读规则】（opening） | 模板 `01_写作规则/05_开篇三章设计指南.md` | 取节：一、三章功能定位（不可互换） / 二、前三章额外限制（在 00_通用写作规则 之上加严） / 三、主角人设落地规则（开篇三章专用） / 四、世界观展开节奏（三层冰山） / 五、与后续章节的衔接规则 | 开篇三章设计指南；2026-09-06 Phase 2：去「使用范围」「七、适配说明」(meta) 与「六、契约自检清单」——后者已由 opening_contract resolver 单独内联进【输出后自检】，整份内联是重复。省 ~2.9 KB / ch1-3 |
| 【必读规则】 | 本工具撰写 `no_invent` | authored | 禁脑补硬约束；逐字取自「示例去污染规则」第 6 条 + 「与正文阶段任务的关系」§6 |
| 【已有数据】 | 本章开篇状态 | 整份 | 本章开篇状态（派生视图，禁止在正文中改写这些初值） |
| 【已有数据】 | 本章细纲 | 整份 | 本章细纲（逐场景执行，不跳过不合并） |
| 【已有数据】 | 主角档案 | 整份 | 主角档案 |
| 【已有数据】 | 计算 `cast_cards_outline` | 取节：【基础档案】 / 【修行档案】 / 【角色内核】 / 【与主角关系】 / 【创作标签】 | 出场对象卡（仅人物；势力/地理卡仍整份），取自细纲「## 出场对象」表。2026-09-06 Phase 2：去【独立故事线】(整条弧线/高光时刻/预计退场——跨章剧透，写本场用不到) 与「动态字段清单」(状态机基线，已在开篇状态里)；保留【创作标签】(说话风格/标志性细节，写对白要)。省 ~8 KB |
| 【已有数据】 | 计算 `wr_hard_manuscript` | resolver | 世界基本法则 · 硬规则清单 |
| 【已有数据】 | 计算 `dy_fh_outline` | resolver | — |
| 【任务】 | 计算 `task_directive` | resolver | 执行要求；逐字取自 01_系统指令.md 任务2 围栏内容，文件名指代改写为区块标题 |
| 【任务】 | 计算 `scene_budget` | resolver | 本章场次与字数预算 |
| 【输出格式】 | 本工具撰写 `output_format_manuscript` | authored | 正文与附录 |
| 【输出后自检】 | 模板 `01_写作规则/00_通用写作规则_校验版.md` | 整份 | 单章一站式自检清单 |
| 【输出后自检】（opening） | 计算 `opening_contract` | resolver | 开篇三章契约自检 |
| 【输出后自检】 | 本工具撰写 `noinvent_selfcheck` | authored | 脑补自检 |
