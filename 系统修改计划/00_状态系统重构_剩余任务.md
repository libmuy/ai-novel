# 状态系统重构 · 剩余任务清单

> 生成时间: 2026-08-31
> 上游文档: `00_状态系统重构执行计划.md`（原始计划）、`00_状态系统重构执行报告.md`（OpenCode 执行结果）
> 本文档只记录**当前仍未完成**的部分，已完成项不重复展开，仅在"已完成"一节简要列出并给出提交记录。

---

## 一、当前状态快照

**提交记录**（`00_状态系统重构执行报告.md` 交付的工作此前一直未提交，本轮已补齐并拆成 4 个逻辑提交，另加 1 个人物卡拆分提交）：

```
5b66bab refactor(苍玄): 拆分人物索引中内嵌的5张核心配角卡
dd8db62 docs: 归档状态系统重构执行计划与执行报告
284166b docs(状态): 路径引用同步 + 去污染文案 + 描述合并缓存机制说明
45c5ceb refactor(状态): W2 解析/合并语义收紧 + W3 描述合并缓存 + W6 部分加固
e77bc02 refactor(状态): W0 测试基线 + W1 目录搬迁与去污染（结构部分）
```

**验证基线**：`bash 02_工具/00_系统级/run_tests.sh` 18/18 通过；`audit_consistency.py` 零 error，5 条 warning + 3 条 info（详见下表，均对应本清单中的待办项，没有意外新增）：

| severity | check | 说明 | 对应任务 |
|---|---|---|---|
| warning | state_baseline_missing | 冻结基线为空，苍玄尚未初始化 | 见「四、苍玄基线重建」 |
| warning | stale_placeholder（地名/类型/伏笔，共 57 处） | 定稿分类里仍有占位符未回补 | W6.6 |
| warning | id_format | 25 个 ID 在多文件出现，需人工确认是否重复定义 | W6.6 附带核查，非阻塞 |
| info | state_unmerged_chapters | 章0001 履历尚未并入最新状态（因基线为空，暂时无法并入） | 见「四」 |
| info | id_frequency_signal / todo_registry | 仅供参考，无需操作 | — |

---

## 二、已完成（本轮新增，此前 W0-W3+W6部分见执行报告）

1. **文档同步**：`03_字段词表.md`、卡片模板、`03_章节状态对账.md`、`06_章节回溯修改.md`、`AGENTS.md` 里所有"冻结回写履历"的旧描述，改成实际的描述合并缓存机制说明。
2. **纠正一处误删**：恢复被误删的 `00_系统架构评估报告与优化建议.md`（按原计划应保留+加历史存档警告，不应删除）。
3. **纠正一处误判**：原计划要求删除 `migrate_state_layout.py`，实际发现它是 `audit_consistency.py` 里 `legacy_global_state_flat` 检查的补救路径引用对象，予以保留。
4. **人物索引去混合**：`02_数据库/07_人物/07_人物.md` 原本是"总索引 + 5 张完整人物卡"的混合体（铁妞/墨七/沈清辞/血罗汉/周莽内嵌在索引文件里），与其余 5 个数据库分类的组织约定不一致。已拆成 5 个独立 `07_人物_<姓名>.md`，索引文件收敛为纯索引表。内容逐字核对与原文一致，未编造任何设定。现在 `07_人物/` 下共 **15 张配角卡**（另有主角苏砚在 `01_设定/00_主角档案.md`，不在此目录）。

---

## 三、W4 · 派生视图物化 + 动态字段清单 + 引用完整性

> 进度（2026-09-01）：**3.1 / 3.2 / 3.3 全部完成；苍玄基线已初始化（见「四」）；W5 关系建模已完成（见「五」）**。剩 W6 加固项。
> - 3.1：19 人物卡 + 28 势力卡 + 主角档案全部加「## 动态字段清单」（commit `8783cb9` + `322f031`）；`02_工具/01_小说通用工具/build_baseline.py` 已建。
> - 3.2：`build_state_snapshot.py --write-chapter-openers` 已实现（`state_tree` 加 `parse_chapter_cast`/`plan_path_for_chapter`/`protagonist_state_id`/`render_chapter_opener`）；`07_单章细纲模板` 加「## 出场对象」小节；`merge_chapter_state.py`/`rebuild_global_state.py` 收尾自动刷新；audit 加 `STATE025 chapter_opener_drift`（warning）；骨架模板加 `03_本章开篇状态.md` 占位；2 条单测覆盖。
> - 3.3：audit 新增 `state_registry` 规则 —— `STATE022 state_object_unregistered`(error) / `STATE023 state_field_undeclared`(error) / `STATE024 state_object_stateless`(info)；`05_工作区/00_全局/00_状态对象白名单.md` 已建（骨架模板同步）；4 条单测覆盖。

### 3.1 数据库卡片声明动态字段（✅ 完成）

现状：`03_字段词表.md` 按类目统一定义字段（所有角色都是同一套 10 个字段），与具体卡片没有关联，无法校验"状态里的对象是否真实存在"——这正是叶云生污染能潜伏到本次重构才被发现的原因。

待做：
- 在每张数据库卡片末尾加"动态字段清单"小节（字段名 + 类型 + 基线初值），格式见执行计划 W4.2
- **逐张读取判断**：
  - 人物卡 15 张（`02_数据库/07_人物/`）+ 主角苏砚（`01_设定/00_主角档案.md`）
  - 势力卡 28 张（`02_数据库/03_势力组织/`，此前报告写"29 张"，实测为 28，以实测为准）
  - 判断标准：哪些属性会随剧情变化需要进入状态追踪（境界、内力值、持有物品……），哪些是纯静态设定（出身、外貌基调……）
  - **不确定某字段初值时留空并报给用户，不要编**——这是内容创作职责边界
- 新增 `02_工具/build_baseline.py`：扫全部卡片的动态字段清单 → 生成基线状态树（把技能 `08_基线状态初始化` 从纯人工变半自动）

### 3.2 逐章开篇状态物化（✅ 完成）

- `build_state_snapshot.py --write-chapter-openers <小说目录>`：为每章生成/刷新 `03_本章开篇状态.md`（值拷贝派生视图，头注「禁止手工编辑」）。第 N 章开篇状态 = 基线 ⊕ 折叠早于第 N 章的全部章履历，再按第 N 章单章细纲「## 出场对象」清单裁剪；清单缺失写全量并在文件头告警。
- 单章细纲模板加 `## 出场对象` 小节（`| 对象ID | 出场方式 | 备注 |`）；`state_tree.parse_chapter_cast` 把 `@主角`/`@人物.[X]`/`@势力.[X]`/`@物品.[X]`/`@财务.[X]`/`@世界.[X]` 映射为状态对象 ID。
- `merge_chapter_state.py` / `rebuild_global_state.py` 写完 `01_最新状态/` 后自动调用 `write_chapter_openers`。
- audit：`STATE025 chapter_opener_drift`（warning）—— 存盘 `03_本章开篇状态.md` 与重算结果不符即报。
- 骨架模板 `05_工作区/01_第01部/01_卷01/章0001/` 加 `03_本章开篇状态.md` 占位。

### 3.3 引用完整性校验（✅ 完成）

audit 新增 `state_registry` 规则（`02_工具/01_小说通用工具/audit/rules/state_registry.py`）：
- `STATE022 state_object_unregistered`（error）：状态对象必须能在对应数据库分类找到同名卡片；群像走 `05_工作区/00_全局/00_状态对象白名单.md` 放行
- `STATE023 state_field_undeclared`（error）：角色/势力对象的状态字段必须在该卡片「## 动态字段清单」声明过
- `STATE024 state_object_stateless`（info，聚合一条）：卡片声明了动态字段但对象从未进入状态

苍玄现状（基线尚空）：STATE022/023 无命中，STATE024 列出全部 48 对象（正常，基线重建后卷1对象会脱表）。

---

## 四、苍玄基线重建（✅ 完成 2026-09-01）

已按技能 `08_基线状态初始化.md` 建**最小基线**（7 对象）：`角色.苏砚`/`角色.柳禾`/`角色.马铁秤`/`势力.黑石会`/`物品.古玉「老灰」`/`物品.矿钉`/`财务.苏砚`。范围＝第01章开篇确有状态且卷1被主角博弈的对象；其余卷1配角（铁妞第2章、周莽第4章等）与新对象（屠铁砧/钱规/沙棘）走首次登场章履历「新建」。

- 用户已定的开局判断：苏砚 凡人 / ≈10 碎灵石（赤贫）/ 矿工聚居区棚户·日间下矿 / 古玉章内由母亲交予（章0001 细纲场景3，即"开篇未戴"）/ 母亲肺痨无续命丹约 5-6 个月。
- 顺带完成：`物品卡` 古玉「老灰」/矿钉/骨针（`04_资源_法宝` RES-FAB-007~009）；`07_人物_铁妞.md` 境界矛盾订正（凡人 → 金刚道·炼皮境）。
- `01_最新状态/` == 基线（尚无章履历并入）；`state_baseline_missing` 已消除。
- 顺带修 audit bug：`_check_changelog_parseable`（STATE011）未跳过 `NN_类目.md` 索引与 `00_*.md`，会把 3 列索引行当状态表报错——已对齐 `load_state_tree` 的跳过规则。

**待细化项**（正文推进后由履历补）：苏砚 `内力值`（入道后）/`外貌变化`/`财务.苏砚 灵石结余`（进入下品灵石量级后）。

---

## 五、W5 · 关系建模（✅ 完成 2026-09-01）

设计决策文档：`00_状态系统重构_W5关系建模_设计checkpoint.md`（含用户 6 项裁决）。

已交付：
- **非对称关系**：物品归属只记 `物品.Y 的「持有者」`；新增 `角色.X 的「师承」`、`势力.X 的「隶属」`
  两个非对称枚举字段（按需声明）。**废除 `角色.X 的「持有物品」`**——反向由脚本扫
  `物品.* 的「持有者」` 生成派生视图 `05_工作区/00_全局/01_最新状态/00_持有物品反查.md`
  （`state_tree.render_holdings_reverse_index`，`write_state_tree(manifest=True)` 时写）。
- **对称关系**：一等对象 `关系.<甲>&<乙>`（ASCII `&` 恰一个、两端 Unicode 排序）。
  `state_tree.py`：`CATEGORY_BY_PREFIX/ORDER/LABELS` 加 `06_关系`；新增
  `split_relation_id` / `normalize_relation_id` / `RelationIdError` / `cast_contains`。
  `parse_chapter_cast` 认 `@关系.[X&Y]`；快照按「至少一端在场」拉取关系对象。
- **废除** `关键关系`（角色）/ `主要关系`（势力）描述字段，内容迁入关系对象。
- `03_字段词表.md` 新增第 6 节「关系类对象」（关系性质 17 值域 / 亲疏 7 档枚举 /
  公开程度 / 甲对乙态度 / 乙对甲态度 / 关系概述 / 对象终态）。
- audit 新规则 `relation`（`02_工具/01_小说通用工具/audit/rules/relation.py`）：
  `RELATION001 relation_id_malformed`（error）/ `RELATION002 relation_dangling`（warning）。
  `state_registry` 加 `关系.` 两端存在性校验；`state.py` `STATE_OBJECT_PREFIX_CATEGORY` 加 `关系`。
- 模板同步：`11_状态台账` / `14_本章状态履历` / `15_全局状态` / `07_单章细纲` /
  `00_使用说明` / `03_章节状态对账` / `08_基线状态初始化` / `AGENTS.md` / 骨架 `00_说明.md`。
- 苍玄基线加 `关系.柳禾&苏砚`（母子）；`关系.苏砚&马铁秤`（压迫/敌对）留章0001履历「新建」。
- 单测 +9（`TestRelationId` / `TestHoldingsReverseIndex` / 4 条 `RELATION` 引擎测试）。共 41 测试全绿。

**未做（W5.1 可选后续）**：`财务.X 的「人情债·债务」` 结构化为指向债权人的列表（当前自由文本条目；
不是双重记录，收益小成本高，暂缓）。`关系性质` / `亲疏` 枚举值域校验归 W6.3（`enum_out_of_domain`）。

---

## 六、W6 剩余加固项（可独立执行，不阻塞 W4/W5）

| # | 问题 | 状态 | 说明 |
|---|---|---|---|
| W6.1 | manifest 权威性 | 待做（设计较复杂） | `00_同步状态.md` 自称非权威却被 drift 检查当真相源；当它是 `__none__` 时 drift 检查退化成"拿基线比基线"必然通过。改法：manifest 记录折叠结果内容哈希，drift 做三方比对（重算哈希 vs manifest 哈希 vs 存盘树哈希） |
| W6.2 | 写锁 | 待做（需并发场景验证） | 全部工具目前无 `flock`；写入整棵树不原子。改法：`fcntl.flock` + 「写 `.new/` → 校验 → `os.replace` → 删旧」 |
| W6.3 | 枚举值域 | 待做（需读修炼体系数据） | `境界` 等枚举字段目前可被写成任意字符串，无值域校验。需给每个枚举字段声明值域（引用 `02_数据库/01_修炼体系`），audit 新增 `enum_out_of_domain` / `enum_regression` |
| W6.6 | 占位符残留 | 待做（需逐条核对，低优先级） | `@地名.[TODO-*]` 21 处、`@类型.[TODO-*]` 14 处、`@伏笔.[TODO-*]` 22 处，源分类均已"定稿"。走 `replace_todos.py`/`auto_link_placeholders.py` 回补，**逐条核对，不批量瞎替换** |

---

## 七、建议执行顺序

```
W4（动态字段清单 + 派生视图 + 引用完整性）
  │
  ├──→ 苍玄基线重建（内容创作，用户参与判断初值）
  │
  └──→ W5（关系建模）

W6.1 / W6.2 可在 W4/W5 之后作为加固措施，与主线并行不冲突
W6.3 / W6.6 优先级最低，可在正式批量出章前完成
```

**下一步动作**：W4 第一件事是逐张读苍玄的 15 张人物卡 + 28 张势力卡 + 主角档案，先摸一遍现状（哪些字段已隐含在设定文本里、哪些完全没有数据），产出一份待确认清单交给用户判断，而不是直接编数据。
