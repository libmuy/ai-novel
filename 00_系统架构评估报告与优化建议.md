# AI小说生成系统架构合理性评估与千万字扩展性优化报告（全量整合落地版·修订版）

> 本修订版在原报告基础上，将【建议二：工作区三级树状分片与作用域继承机制】及其对应的【落地计划2】整体替换为**章级链式状态增量方案**，以解决原方案中"03_规划"与"05_工作区"两棵目录树在部/卷层重叠、状态文件仍趋于臃肿等问题。其余五项建议维持原方案不变。

## 摘要

本报告对当前 AI 小说生成系统的架构进行了系统、深入的审查与量化评估。评估涵盖系统三层职责划分、数据隔离与软链接复用、Agent 任务路由表与 Token 控制策略、状态追溯与一致性对账机制，并重点分析了系统在**大模型上下文 Token 效率**以及**支持千万字（10,000,000+ 字 / 3,300+ 章）超长篇小说**时的瓶颈与扩展性表现。

评估结果表明：**该系统在架构设计上展现出了极高的工程专业度与完备性**，其"三层职责解耦"、"查表型数据离散化"、"语法级 `@引用` 与占位符闭环"等设计属于业界优秀的 AI 文本生成基础设施架构。但在**千万字超长篇小说**场景下，现有设计在**总索引膨胀**、**全量状态台账检索**、**伏笔归属不当**、**全局引用拓扑闭环**以及**跨章节上下文滑窗传递**方面仍存在明确的性能与一致性瓶颈。

本报告完整整合了**所有 6 项核心架构重构与优化建议**，并为**每一条建议均制定了包含目标文件、具体改动步骤、操作指令及验收标准的详细工程落地计划（Execution Plan）**。

---

## 一、 系统架构整体合理性评估

### 1.1 核心优势与合理性设计

1. **清晰的三层职责解耦（设定层 / 规划层 / 创作层）**

  - **设定层（`01_设定/`、`02_数据库/`）**：回答"是什么/规则是什么/谁是谁"，禁止硬编码剧情时间线；
  - **规划层（`03_规划/`）**：回答"什么时候发生"，支持部/卷/章三级大纲与事件卡解耦；
  - **创作层（`10_正文/`）**：仅存放定稿正文，与生产中间过程隔离。
  - **点评**：极大地减轻了大模型在创作时的"设定幻觉"与"剧情越权"，确保了长篇故事框架的稳定。

2. **数据隔离与逻辑复用机制**

  - 通过 `00_通用模板` 目录集中管理规则与卡片模板，并在每部小说中通过软链接关联 `00_通用模板 -> ../../00_通用模板`。
  - **点评**：实现了"一套规矩与卡片，多本小说隔离存储"的标准 SaaS / 多租户软件工程架构，避免了多项目数据混淆与重复冗余。

3. **语法级关联与自动化一致性审查（`@引用` + ID 编号）**

  - 系统定义了 `@人物.[姓名]`、`@地名.[名称]`、`DY-xxx`、`FH-xxx`、`TODO-xxx` 等严谨的引用解析规范，并配备了 `02_工具/audit_consistency.py` 一站式自检工具。
  - **点评**：将传统的纯文本网文写作升级为"强类型数据链接网络"，为长篇小说的状态一致性校验奠定了工程基础。

---

## 二、 大模型 Context Token 效率量化分析

基于编写的 `02_工具/eval_architecture.py` 评估脚本，对系统典型 Agent 任务在上下文加载方面的 Token 效率进行了实测计算（按平均 1.5 字符 ≈ 1 Token 推算）：

### 2.1 典型任务上下文 Token 占用实测数据

| 任务类型       | 必读模板 / 写作规则          | 必读定稿数据                              | 包含文件数 | 预估 Context Token 占用 | Token 效率评价                      |
| ---------- | -------------------- | ----------------------------------- | ----- | ------------------- | ------------------------------- |
| **写单章正文**  | 系统指令 + 通用写作规则 + 文风模板 | 主角档案 + 小说概念 + 单章细纲 + 出场人物 + 状态台账    | ~8 个  | **~13,930 Tokens**  | **优秀**（在 128k/200k 上下文中占比 <10%） |
| **生成单卷大纲** | 卷大纲模板 + 通用写作规则       | 主角档案 + 核心道义 + 规划总索引 + 体系/地理/势力/资源索引 | ~9 个  | **~15,953 Tokens**  | **良好**（受限于总索引文件大小）              |
| **云端回填分割** | 云端回填分割规则             | 00\_进度.md                           | ~2 个  | **~4,793 Tokens**   | **极佳**                          |

### 2.2 Token 效率优势与潜在问题

- **优势**：
  1. 路由表严格约束单次任务加载模板不得超过 4 个，有效控制了提示词系统的"指令膨胀"；
  2. 查表型数据（如 `01_修炼体系/`、`04_资源/`）采用了"总索引 + 子文件（≤1KB 拆分）"策略，实现了"按需取条"，避免整库灌入。
- **瓶颈**：
  1. **规则重复加载**：`00_通用写作规则.md`（7,415 字，~4,943 Tokens）在几乎所有创作类任务中全量加载，包含很多后置校验才需要的条款；
  2. **静态卡片冗余**：写单章正文时，`00_主角档案.md`（~3,332 Tokens）包含全书 5 个阶段的完整成长弧线与终局设定，写前几卷时加载了大量无用的后段设定。

---

## 三、 千万字（10,000,000+ 字）扩展性与状态一致性分析

根据脚本模拟推算，一部 **1,000 万字**（按 3,000 字/章计算，约 **3,334 章**，划分为 **20 部**、**100 卷**）的修仙/玄幻巨著在系统中的物理节点与数据规模如下：

### 3.1 千万字规模推算模型数据

```
[千万字小说文件与索引规模推算]
├── 项目物理文件总数: ~14,769 个文件
│   ├── 正文文件 (10_正文/): 3,334 个 Markdown
│   ├── 细纲与事件卡 (03_规划/): 3,334 个细纲 + ~5,001 个事件卡
│   └── 数据库实体文件 (02_数据库/): ~3,050 个 (含800角色/300地点/150势力/600资源)
└── 核心索引文件 Token 膨胀压力
    ├── 全书规划总索引 (03_规划/规划.md): ~3,434 行 | 预估 Token: ~345,400 Tokens (超 128k 窗口!)
    ├── 伏笔总索引 (原02_数据库/08_伏笔登记.md): ~1,200 条 | 预估 Token: ~96,000 Tokens (若不分片将严重拥堵)
    └── 状态台账 (04_状态/出场对象台账.md): ~800 角色 | 预估 Token: ~506,666 Tokens (超 200k 窗口!)
```

### 3.2 千万字场景下的核心瓶颈诊断

1. **总索引文件突破上下文极限（死锁风险）**
  - 现行架构规定 `03_规划/规划.md` 存储全书规划总索引。在 3,300+ 章规模下，该文件行数超 3,400 行，大小约 **506 KB**，单文件即需 **~34.5 万 Tokens**。在任何标准 128k/200k 大模型任务中，仅加载规划总索引就会直接导致 **OOM 或上下文截断**。
2. **"伏笔放数据库"违背三层职责规范且检索效率极低下**
  - 原架构将伏笔登记表放在 `02_数据库/08_伏笔登记/`。数据库本应回答"是什么/谁是谁"（静态世界实体），而伏笔核心回答的是 **"在第几卷/第几章埋下，预计在第几卷/第几章回收"**（动态时间线规划）。存放在静态数据库中不仅破坏了三层职责，而且在 1,000+ 条伏笔堆积时，写单章检索关联伏笔的正确率会急剧下降。
3. **状态台账（`04_状态/`）单文件膨胀与对账效率低下**
  - 随着上百卷故事推进，出场人物达到数百人，全局 `出场对象台账.md` 膨胀至 **50 万 Tokens**，且全量线性对账成本极高，本地 Agent 无法高效解析。
4. **占位符（`TODO-xxx`）回补拓扑环路与卡死**
  - 实测运行 `python3 02_工具/audit_consistency.py` 发现：样例 `00_苍玄` 中存在 **107 处** 残留占位符（如 `@势力.[TODO-xxx]`、`@伏笔.[TODO-xxx]`）。在千万字长篇中，如果下游任务回补速度赶不上上游生成速度，占位符图谱将剧烈膨胀，导致一致性校验工具瘫痪。

---

## 四、 6 项架构优化建议设计与格式规范

本节详细列出了针对所有架构瓶颈的 6 项优化方案及具体落地的目录树与文件规范。

---

### 4.1 建议一：将"伏笔"彻底从数据库（`02_数据库/`）迁入规划层（`03_规划/`）

#### 1. 架构合理性分析

| 对比维度         | 放在设定/数据库 (`02_数据库/`) [旧设计]                           | 放在规划层 (`03_规划/`) [重构落地设计]                                          |
| ------------ | ---------------------------------------------------- | ------------------------------------------------------------------ |
| **职责对齐**     | **违背职责**。数据库代表静态世界实体，伏笔不是客观实体，而是剧本推进安排。              | **完全契合**。规划层回答"什么时候发生"，伏笔的核心就是"何时埋设、何时触发、何时回收"。                    |
| **生命周期**     | 与角色、地理等永久实体混在一起，无法随剧情阶段自动归档。                         | 随 部/卷 结构天然分片。卷结案后，已回收伏笔自动进入历史卷规划，不占用新卷上下文。                         |
| **Token 效率** | 千万字 1,200 条伏笔全部塞在 `02_数据库/08_伏笔登记.md`（~10 万 Tokens）。 | 按卷离散在 `03_规划/<部>/<卷>/00_伏笔册_卷XX.md`，单卷仅包含 10~20 条动态伏笔（<1k Tokens）。 |

#### 2. 卷级伏笔册落方案示例：`03_规划/01_第01部/01_卷01/00_伏笔册_卷01.md`

```
# 第01部卷01：边矿风云 · 伏笔跟踪册

## 1. 本卷新埋伏笔
| 伏笔ID | 伏笔名称 | 埋设位置 | 拟回收卷/章 | 伏笔内容描述 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| FH-V1-001 | 锈铁剑异响 | 卷01章0003 | 卷01章0025 | 主角在废矿坑捡到的锈铁剑在月圆夜有微弱鸣响 | 活跃(已埋设) |

## 2. 本卷拟回收/触发伏笔
| 伏笔ID | 来源埋设位置 | 本卷回收章节 | 回收触发事件 | 执行状态 |
| :--- | :--- | :--- | :--- | :--- |
| FH-V1-001 | 卷01章0003 | 卷01章0025 | 主角生死关头触发剑内残魂响应 | 待执行 |
```

---

### 4.2 建议二（修订）：工作区章级链式状态增量与结构化字段合并机制

> **修订说明**：原方案将 `04_状态/` 与 `05_任务/` 合并为 `05_工作区/`，并按"部/卷/章"三级建立"作用域继承"（部级公共状态 → 卷级公共状态 → 章级增量逐层覆盖）。该方案在实际推演中发现两个问题：其一，"部级/卷级公共状态"与建议三中 `03_规划/<部>/<卷>/` 的规划索引在目录层级上重叠，需要维护两棵平行目录树，容易路径不同步；其二，"部级/卷级公共状态"本质仍是某个范围内的全量快照，规模越大依然趋于膨胀。现改为**只在"章"这一层记录状态，采用链式增量**：每一章的初始状态 = 上一章初始状态 + 上一章履历合并，不再设部级/卷级中间状态节点。

#### 1. 架构升级说明

将原 `04_状态/`（状态）与 `05_任务/`（生成任务）统一融合升级为 **`05_工作区/`（Workspace）**，工作区内**只保留"章"一级目录**，不设部级/卷级公共状态文件：

```
01_小说数据/<NN>_<小说名>/
├── 01_设定/                            # [设定层] 静态世界与主角规则
├── 02_数据库/                          # [资料层] 离散实体卡
├── 03_规划/                            # [规划层] 部 / 卷 / 章 大纲规划 + 伏笔册 + 事件卡
├── 05_工作区/                          # [动态工作区] 生产任务与状态对账
│   ├── 00_全局/                        # 通用技能与工具
│   ├── 01_第01部/
│   │   ├── 01_卷01/
│   │   │   ├── 01_章0001/              # [章级作用域·唯一的状态节点]
│   │   │   │   ├── 01_提示词.md        # 本章下发 Prompt
│   │   │   │   ├── 02_云端回填.md      # 云端原始输出
│   │   │   │   ├── 03_本章初始状态.md  # 【结构化】继承自上一章合并结果
│   │   │   │   ├── 04_本章状态履历.md  # 【结构化】本章新产生的状态变化
│   │   │   │   └── 05_校验结果.md      # 本章校验 Log
│   │   │   ├── 02_章0002/
│   │   │   └── 99_卷末状态快照.md      # 【只读·物化视图】仅供审计/检索，不参与继承链
│   │   └── 02_卷02/
│   └── 02_第02部/
└── 10_正文/                            # [创作层] 纯定稿正文 (只读)
```

#### 2. 字段分类与合并语义（核心设计原则）

状态字段不再区分"部级/卷级/章级作用域"，而是按**字段的合并方式**分为两类，类型登记于 `00_通用模板/03_字段词表.md`：

| 类型 | 定义 | 合并规则 | 举例 |
|---|---|---|---|
| **运算类字段** | 数值、枚举、列表 | 脚本按固定语法做加减/替换/增删运算 | 内力值、境界、持有物品、好感度 |
| **描述类字段** | 自由文字 | 永远整体覆盖，不做语义合并，取本章最新值 | 外貌变化、当前心境、人物关系备注 |

运算类字段在"履历"文件中必须使用固定语法：数值增减写 `+N`/`-N`，枚举/数值替换直接写目标值，列表增删写 `+X,-Y`，对象终态使用固定枚举（`活跃`/`死亡`/`退场`/`暂离`）。

`03_本章初始状态.md` 与 `04_本章状态履历.md` 统一采用如下结构化表格：

```markdown
| 对象ID | 字段 | 类型 | 值 |
| --- | --- | --- | --- |
| 角色.叶云生 | 内力值 | 运算-数值 | -30 |
| 角色.叶云生 | 境界 | 运算-枚举 | 炼气三层 |
| 角色.叶云生 | 持有物品 | 运算-列表 | +锈铁剑,-破草鞋 |
| 角色.叶云生 | 外貌变化 | 描述 | 左臂多了一道疤痕 |
```

（`03_本章初始状态.md` 中"值"列记录合并后的当前实际值；`04_本章状态履历.md` 中"值"列记录本章发生的变化量。）

#### 3. 章后自动合并流程

写单章正文时，LLM 继承的状态上下文仅为：`全书设定库` ➔ `本章初始状态.md`。章节定稿后：

```
第N章定稿
   │
   ├─ 解析 第N章/04_本章状态履历.md → 结构化数据
   ├─ 读取 第N章/03_本章初始状态.md → 结构化数据
   ├─ 按字段类型合并：运算类做加/减/替换/增删；描述类直接覆盖
   ├─ 未在履历中出现的对象/字段保持原样
   └─ 输出合并结果 → 写入 第N+1章/03_本章初始状态.md
```

终态标记为"死亡"/"退场"的对象，后续章节默认不再展开继承，以控制文件体积（除非履历中再次显式引用，如特殊剧情需要）。

#### 4. 审计与检索：卷末只读快照

链式增量的代价是查询"很久未出场角色的当前状态"需要顺链回溯。为此在每卷末自动生成一次只读物化视图 `99_卷末状态快照.md`，重放该卷所有章节的初始状态与履历得到全量汇总，**仅供人工查阅与 `audit_consistency.py` 校验使用，不参与写作时的正常继承链**。

---

### 4.3 建议三：规划层实施"三级树状分片索引（Hierarchical Index Sharding）"

#### 1. 顶级规划总索引：`03_规划/规划.md`

```
# 苍玄纪 · 全书顶层部规划总索引

| 部编号 | 部名称 | 覆盖卷范围 | 核心剧情节点 | 部规划链接 |
| :--- | :--- | :--- | :--- | :--- |
| 第01部 | 灰壤崛起 | 卷01~卷05 | 主角从废墟凡域破局，建立初始势力 | [规划_第01部.md](./01_第01部/规划_第01部.md) |
| 第02部 | 紫极天道 | 卷06~卷12 | 进入上界大宗争霸，揭开法则真相 | [规划_第02部.md](./02_第02部/规划_第02部.md) |
```

*(无论全书写到 3,000 章还是 10,000 章，顶级 `规划.md` 大小恒定 < 2KB Tokens)*

> **与修订后建议二的关系**：`03_规划/` 只存放大纲、部/卷规划与伏笔册这类"计划性"内容；`05_工作区/` 只存放"章级状态"这类"执行过程"内容。两者不再共用部/卷层的中间节点文件，避免路径重叠。

---

### 4.4 建议四：提示词与写作规则实施"阶段剪裁（Task-Tailored Pruning）"

拆分规则与主角档案，写单章时仅按需加载：

- `00_通用写作规则_生成版.md`（~2.5k Tokens，屏蔽校验后置规则）；
- `01_设定/00_主角档案_当前阶段.md`（仅提取主角当前卷对应的阶段成长性格与能力）。

---

### 4.5 建议五：强化 `02_工具/` 自动化 Agent 规则与拓扑回补机制

编写并升级自动链路回补脚本 `02_工具/auto_link_placeholders.py`，建立 `TODO-xxx` 到已定稿实名 ID 的字典自动替换逻辑，防止占位符在千万字生成过程中卡死校验。

---

### 4.6 建议六：建立"滑动窗口上下文（Sliding-Window Context）"正文衔接机制

在每个单章细纲文件中，由 Agent 自动维护 **"前置衔接滑动窗口"**：

```
# 卷01第0015章：夜入黑市 · 单章细纲

## 0. 上下文滑动窗口 (Sliding Window Context)
### 上章末尾摘要 (300字)
叶云生在灰壤凡域九曲长街利用劣质培元丹避开了赵铁柱的第一次盘查，但身份已引起黑石会怀疑，决定潜入墨骨巷。

### 上章结尾原文最后 500 字
夜色渐浓，九曲长街的灯火在雨幕中泛着冷冽的光。
叶云生将兜帽再次拉低，左臂阻滞的经脉隐隐作痛。"还能撑三个时辰。"他低语一句，闪身没入了雨巷深处……
```

---

## 五、 6项架构重构建议的具体落地执行计划（Execution Plans）

为确保上述 6 项重构建议能够在实际工程中准确落地，以下为每一项建议制定的**具体执行计划（Execution Plan）**：

### 5.1 落地计划 1：伏笔归入规划层重构计划

- **目标文件与目录**：
  * 弃用：`00_通用模板/02_卡片模板/05_伏笔登记表模板.md`，从 `02_数据库/` 中移除 `08_伏笔登记/` 分类。
  * 新增：`00_通用模板/02_卡片模板/05_卷级伏笔跟踪册模板.md`；
  * 存放位置：修改为 `01_小说数据/<NN_小说名>/03_规划/<部编号>/<卷编号>/00_伏笔册_卷XX.md`。
- **具体实施步骤**：
  1. **规则与路由更新**：修改 `00_通用模板/00_使用说明.md` 中的路由表与引用规范，将"伏笔登记"的必读数据路径由 `02_数据库/08_伏笔登记` 改为 `03_规划/《部》/《卷》/00_伏笔册_《卷》.md`；
  2. **模板迁移**：创建 `05_卷级伏笔跟踪册模板.md`，定义"本卷新埋伏笔"与"本卷拟回收/触发伏笔"两张表；
  3. **存量数据迁移脚本**：编写迁移脚本 `02_工具/migrate_foreshadowing.py`，将既有 `02_数据库/08_伏笔登记.md` 中的条目按"埋设卷号"自动拆分归入对应的 `03_规划/<部>/<卷>/00_伏笔册_卷XX.md` 中。
- **验收标准**：
  * 执行 `python3 02_工具/audit_consistency.py` 不再检测 `02_数据库/08_伏笔登记/` 目录，全书伏笔按卷物理离散存放且 `@伏笔.FH-xxx` 解析正常。

---

### 5.2 落地计划 2（修订）：章级链式状态增量与结构化合并机制实施计划

- **目标文件与目录**：
  * 骨架重构：修改 `00_通用模板/05_项目骨架模板/`，将原 `04_状态/` 与 `05_任务/` 合并为 `05_工作区/`，**目录只到"章"一级，不设部级/卷级公共状态文件**；
  * 新增模板：`03_本章初始状态.md`、`04_本章状态履历.md` 空白模板；
  * 新增字段词表：`00_通用模板/03_字段词表.md`，登记所有合法字段名、适用对象类型、字段类型（运算-数值/运算-枚举/运算-列表/描述）。
- **具体实施步骤**：
  1. **字段词表冻结**：先梳理全书涉及的状态字段（角色、势力、物品、地点等各类对象），确定运算类与描述类的划分，登记进字段词表，作为后续所有脚本的唯一权威依据；
  2. **架构规范更新**：更新 `00_系统架构规范.md` 与 `AGENTS.md`，将"部级/卷级公共状态"相关描述替换为"章级链式增量"设计；
  3. **提示词改造**：正文生成任务的提示词中明确要求 Agent 在产出正文的同时，必须按固定表格格式产出 `04_本章状态履历.md`，字段名须来自字段词表，运算类字段须遵守固定语法（`+N`/`-N`/`+X,-Y`/固定枚举）；
  4. **Markdown 表格解析函数**：编写 `parse_md_table()`（纯标准库实现，无需第三方依赖），将表格解析为 `[{对象ID, 字段, 类型, 值}, ...]` 结构；
  5. **章后合并脚本**：编写 `02_工具/merge_chapter_state.py`：
     - 读取第N章 `03_本章初始状态.md` + `04_本章状态履历.md`；
     - 按 `(对象ID, 字段)` 做合并：运算类按语法做加减/替换/增删列表，描述类直接覆盖；未提及的对象/字段原样保留；
     - 写入第N+1章 `03_本章初始状态.md`；
     - 提供 `--dry-run` 模式（只打印 diff 不写入）、自动备份 `.bak`、合并日志；
     - 要求脚本**幂等**：对同一章重复执行结果一致。
  6. **卷末快照脚本**：编写 `02_工具/build_state_snapshot.py`，在每卷完结时重放该卷全部章节的初始状态与履历，生成只读的 `99_卷末状态快照.md`，仅供审计/人工查阅，不参与写作继承链；
  7. **存量数据迁移**：将原 `04_状态/出场对象台账.md` 中的现有数据，按对象最后一次出现的章节，拆分写入对应章的 `03_本章初始状态.md` 中，作为链的起点。
- **验收标准**：
  * 任意一章的 `03_本章初始状态.md` 文件大小与本章实际涉及对象数量成正比，不随全书章节数增长而单调膨胀；
  * `merge_chapter_state.py --dry-run` 对同一章连续执行两次，输出 diff 完全一致（幂等性验证通过）；
  * 人为构造一组包含数值、列表、描述、终态标记字段的履历表，脚本合并结果与人工手算结果完全一致；
  * 卷末快照脚本生成的 `99_卷末状态快照.md` 中，任一角色状态与逐章重放手工核算结果一致；
  * `audit_consistency.py` 能够识别并报错"字段词表中不存在的字段名""履历表语法不合法"两类问题。

---

### 5.3 落地计划 3：规划层三级树状分片索引实施计划

- **目标文件与目录**：
  * 模板：修改 `00_通用模板/02_卡片模板/06_卷大纲模板.md`；
  * 数据：重构 `03_规划/规划.md`，并在每部、每卷下建立 `规划_第XX部.md` 与 `规划_卷XX.md`。
- **具体实施步骤**：
  1. **顶级索引瘦身**：修改 `规划.md` 模板，规定其仅能填写"部（Part）"级行信息，禁止写入任何章级信息；
  2. **中间部级索引生成**：在任务 09（全书大纲生成）产出时，自动在 `03_规划/<部编号>/` 目录下生成 `规划_第XX部.md` 索引本部的卷列表；
  3. **卷级大纲落地**：任务 10（卷大纲生成）产出落位在 `03_规划/<部编号>/<卷编号>/规划_卷XX.md`。
- **验收标准**：
  * 评估脚本计算千万字规模下顶级 `03_规划/规划.md` Token 占用 <2,000 Tokens。

---

### 5.4 落地计划 4：提示词与写作规则"阶段剪裁"实施计划

- **目标文件与目录**：
  * 规则拆分：创建 `00_通用模板/01_写作规则/00_通用写作规则_生成版.md` 与 `00_通用写作规则_校验版.md`；
  * 动态卡片：建立 `01_小说数据/<NN>/01_设定/00_主角档案_当前阶段.md`。
- **具体实施步骤**：
  1. **写作规则精简**：从原 `00_通用写作规则.md` 中剥离"一站式自检清单/后置排查项"至 `校验版`，`生成版` 仅保留前置写作指导（降低 50% 篇幅）；
  2. **主角卡切片器**：编写脚本 `02_工具/prune_character_sheet.py`，根据当前卷号（如卷 1~2 对应第一阶段），提取主角档案中对应阶段的内容写入 `00_主角档案_当前阶段.md`；
  3. **路由表修改**：更新 `00_使用说明.md` 路由表，正文生成任务绑定 `生成版规则` 与 `当前阶段主角卡`。
- **验收标准**：
  * 正文生成任务的 Context Token 消耗由 ~13.9k Tokens 降低至 ~8.5k Tokens。

---

### 5.5 落地计划 5：自动化 Agent 占位符回补工具落地计划

- **目标文件与目录**：
  * 编写脚本：`02_工具/auto_link_placeholders.py`。
- **具体实施步骤**：
  1. **建立映射图谱**：脚本读取 `02_数据库/00_TODO全局注册表.md` 与已定稿文件（如 `03_势力组织/` 下的文件），建立字典：`{"TODO-001": "@势力.[云璃商会]", "TODO-002": "@地名.[墨骨巷]"}`；
  2. **正则批量替换**：遍历 `02_数据库/` 及 `03_规划/` 中所有含 `@类型.[TODO-xxx]` 的文件进行实名替换；
  3. **勾选表格**：自动将文本末尾【待创建条目】列表中的 `- [ ] @类型.[TODO-xxx]` 改为 `- [x] @类型.[TODO-xxx]`；
  4. **集成进校验链**：在 `audit_consistency.py` 中添加 `--auto-fix` 参数，自动调用该工具。
- **验收标准**：
  * 运行 `python3 02_工具/auto_link_placeholders.py 01_小说数据/00_苍玄` 后，107 处残留占位符全部自动匹配回补，`audit_consistency.py` 告警归零。

---

### 5.6 落地计划 6：跨章滑动窗口衔接机制实施计划

- **目标文件与目录**：
  * 模板更新：修改 `00_通用模板/02_卡片模板/07_单章细纲模板.md`，在开头增加 `## 0. 上下文滑动窗口 (Sliding Window Context)` 字段；
  * 自动化写入：更新本地 Agent 章后处理逻辑。
- **具体实施步骤**：
  1. **模板规范升级**：在单章细纲模板头部定义 `上章末尾摘要 (300字)` 与 `上章结尾原文最后 500 字` 两个条件必填区块；
  2. **章后自动填入**：当第 N 章正文生成结束并定稿后，本地 Agent 自动调用摘要工具生成 300 字摘要，截取第 N 章正文最后 500 字，写入第 N+1 章细纲文件的"滑动窗口"区块中；
  3. **Prompt 引用注入**：在正文生成提示词中明确指示："请紧密接续滑动窗口中的上章末尾场景与文字口吻进行续写"。
- **验收标准**：
  * 每一章细纲文件均自动包含前章滑窗数据，连续章节间的场景与临场语气过渡无缝衔接。

---

## 六、 落地优先级建议

六项建议改动范围不同，建议分阶段推进，而非一次性铺开：

1. **先行（当前即有收益、风险低）**：建议五（占位符回补，已有 107 处积压）、建议一（伏笔迁移，职责错位问题现在就存在）；
2. **其次（收益随规模增长、风险中等）**：建议三（规划索引分片）、建议四（阶段剪裁）；
3. **最后（改动范围最大，建议先在单个部/卷试点）**：建议二（章级链式状态增量）——先冻结字段词表并在"00_苍玄"的某一卷小范围试跑合并脚本，验证幂等性与合并结果无误后，再迁移存量数据并推广到全书；建议六（滑动窗口）可与建议二并行，二者无强依赖关系。

---

## 七、 总结

通过为上述 6 项重构建议提供明确的 **工程目标、实施步骤、工具脚本与验收标准**，本报告形成了从"架构评估"到"量化分析"再到"工程落地计划"的完整闭环。其中【建议二】经修订后，改为"只在章级记录状态、按字段类型做结构化合并、卷末生成只读审计快照"的链式增量方案，避免了原方案中规划层与工作区目录重叠、状态文件仍趋于膨胀的问题。按此计划分阶段执行重构，系统将能够高效、稳定、无缝地支持 **1,000 万字（3,300+ 章）** 超长篇网文小说的自动化生产。
