# 状态对象白名单

> 允许出现在状态追踪（基线 / `01_状态履历.md` / `01_最新状态/`）中、但**不对应任何数据库卡片**的对象。
> 用于群像、临时、背景对象——有轻量状态需求，但不值得建完整卡片。
> `audit_consistency.py` 的 `state_object_unregistered`（STATE022）据此放行。
>
> **加入前先确认**：这个对象真的不该有卡片吗？拼错的名字、本应建卡的配角，不要往这里塞。
> 一旦某群像对象戏份变重、需要完整设定，应改为建卡并从本表移除。

| 对象ID | 类型 | 说明（为什么不建卡） |
|---|---|---|
| 物品.缺嘴酒葫芦 | 物品 | 周莽随身多年的缺嘴酒葫芦（人物卡【标志性细节】），凡俗日用容器，不具经济/战斗机制价值，够不上 `04_资源_法宝.md` 的法宝范畴，不建 RES 卡；但持有者会跨章变化（ch0004 周莽→苏砚），需要状态追踪防止正文再写反持有人（ch0005 教训：曾在正文里写成"周莽又从腰间扯下同一只葫芦"）。 |
