# 苍玄 · 禁用词表

> 类型：设定层数据。`audit_consistency.py` 的 `manuscript_lexicon` 规则读取本文件，
> 对 `10_正文/**/*.md` 逐行匹配，命中即 `LEXICON001`（warning）。
> 格式见 `00_通用模板/01_写作规则/08_禁用词表说明.md`。
>
> 苍玄＝东方修真·底层矿城世界。计量沿用 里/丈/尺/寸、斤/两/钱、时辰/一炷香/一盏茶；
> 货币为 碎灵石（按枚）/ 下品灵石。以下现代词一律穿帮。
> 初版 2026-09-02：`## 待确认` 区的时间/距离/精确单位是否彻底禁用，待用户拍板。
> 2026-09-06：红线包 §六 列出的 8 个计量词 + `归档` 转正式禁（`公里/厘米/分钟/小时/公斤/百分之`
> 走 `## 包含`，`米/秒` 走 `## 正则` 数字锚定以避开「厘米/秒杀/克扣」类误伤）；其余计量词的
> 同类处理待用户确认。

## 包含

手电            // 现代照明器具；ch1「手电光柱」穿帮，应为灵石灯/火折/夜明矿脉光
电筒            // 同上
手电筒          // 同上
电池            // 现代储能
电线            // 现代
灯泡            // 现代
玻璃            // 苍玄无平板玻璃工艺（琉璃可，须另判）
塑料            // 现代高分子
橡胶            // 现代
尼龙            // 现代
水泥            // 现代建材
混凝土          // 现代建材
沥青            // 现代
油毡            // ＝沥青油毛毡，现代建材；棚户顶/门帘用 草帘/苇席/破麻帘/油布（ch2 通读发现，2026-09-04 归口）
油毛毡          // 同上
不锈钢          // 现代冶金
合金            // 现代冶金术语（灵材/玄铁/精铁可）
钢筋            // 现代
螺丝            // 现代紧固件
弹簧            // 现代（机括/簧片须另择词）
玻璃钢          // 现代复合材料
雷达            // 现代
信号            // 现代通讯语义（灵讯/传音符可）
引擎            // 现代
马达            // 现代
数据            // 现代（古玉设定内部代号 raw data 仅存于设定层，正文禁用）
分析            // 偏现代书面语，正文慎用（"推演/参详"替）
归档            // 偏现代书面语（"收录/入册"替）；红线包 §六 摘录，2026-09-06 用户拍板正式硬禁
系统            // 网文金手指词，正文禁用
面板            // 同上
进度条          // 同上
OK             // 现代口语
拜拜            // 现代口语
搞定            // 现代口语
给力            // 网络词
靠谱            // 现代口语
情绪            // 偏现代书面语（"心绪/心思"替，须另判）
公里            // 现代距离单位；用「里」。2026-09-06 用户拍板由待确认转正式禁
厘米            // 现代长度单位；用「寸/分」
分钟            // 现代时间单位；用「一盏茶/片刻」
小时            // 现代时间单位；用「一个时辰／半个时辰」
公斤            // 现代重量单位；用「斤」
百分之          // 现代比例表述；用「三成／七分」

## 正则

[\d一二两三四五六七八九十百千半几数]\s*米       // 现代长度单位（数字+米）；用「丈/尺」。「厘米/大米/米粒」不误伤
[\d一二两三四五六七八九十半几数]\s*秒           // 现代时间单位（数字+秒）；用「瞬息/眨眼」。「秒杀/分秒」不误伤

## 待确认

千米            // 距离；红线包 §六 未列，同 公里 处理待用户确认
毫米            // 长度；同上
平方            // 面积表述；红线包 §六 未列
立方            // 体积表述；红线包 §六 未列
千克            // 重量；红线包 §六 未列，同 公斤 处理待确认
克              // 重量；子串会误伤「克扣/克制」，须走正则且待确认
吨              // 重量；红线包 §六 未列
摄氏度          // 温度；苍玄是否有精确温标待确认
