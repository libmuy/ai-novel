# 一次性脚本归档

本目录下的脚本已完成其历史使命，**不要在新书或当前"苍玄"项目上再次运行**：

- `replace_todos.py`：硬编码了 `01_小说数据/00_苍玄` 路径，一次性批量替换 TODO 占位符为全局 ID，已执行完毕。
- `migrate_state_layout.py`：一次性状态布局迁移脚本（旧模型 → 每对象一文件模型），已执行完毕。
- `eval_architecture.py`：早期的架构量化评估脚本（Token 效率 / 文件数膨胀推算）。硬编码的样例路径（`00_主角档案_当前阶段.md`、`规划_卷章0001.md`、`07_人物_刘三斤.md`）早已不存在，从未进工具索引、无测试覆盖。留档备查，不要再跑。

如需类似功能，请勿直接复用这两个脚本，应参考其逻辑重新编写一个接受 `novel_dir` 参数的通用版本，放入 `../01_小说通用工具/`。
