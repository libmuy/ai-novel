# 小说创作 Workspace 说明

> **本文档只回答两件事：「去哪读」与「文件边界」。不写任何规则正文。**
> 规则各有其唯一权威位置，见 `00_通用模板/00_系统架构规范.md` §二·A【规则层自身也适用本原则】的权威位置表。
>
> **任务开始前必须先读 `00_通用模板/00_使用说明.md`**——任务路由表、`@引用`/ID 规范、Token 控制、加载边界全部集中在那里。

## 一、工作区结构

- **工作区根目录** = 本仓库根，所有路径相对此目录。
- `00_通用模板/` — 所有小说共享的字段定义与写作规则。**只读，禁止写入任何具体小说数据。**
- `01_小说数据/<NN>_<小说名>/` — 每本小说独立一个子文件夹，数据完全隔离。
- `02_工具/` — 跨小说通用的维护/校验工具；不属于任何一本书，也不属于模板体系。
- 各小说子文件夹内的 `00_通用模板` 是指向根目录的**符号链接**（`-> ../../00_通用模板`），只读。

物理隔离（单本独立文件夹）+ 逻辑复用（软链接关联通用模板）是本工作区的底层架构。

- 目录结构 / 命名规范 / 模板↔数据对应关系 → `00_通用模板/99_速查手册.md`
- 新建小说（编号规则与流程）→ 技能 `03_任务技能/02_小说级/07_新建小说.md`（用 `new_novel.py`，**不要手工 cp 骨架**）

## 二、创作工作流总览

前期规划（风格/主题/章节框架）与正文生成都由**云端大模型**完成，通过浏览器粘贴、附件上传交互；云端不访问本仓库，因此提示词必须**自包含**。本地 Agent 只负责：拼装提示词 → 落位产出 → 跑确定性检查与冷读循环 → 折叠状态。

- 提示词拼装规格（六段骨架、规划任务路由、预建文件、冷读修订轮）→ `00_通用模板/04_提示词/00_云端提示词生成器.md`
- 正文阶段任务指令 → `00_通用模板/01_写作规则/01_系统指令.md`
- 单章质量验收（落位门禁、冷读循环、通读发现归口）→ 技能 `03_任务技能/02_小说级/04_单章质量验收.md`

## 三、任务执行约定

1. **必读入口**：每次任务开始，先读 `00_通用模板/00_使用说明.md`。
2. **架构规范**：首次执行任务前，读 `00_通用模板/00_系统架构规范.md`（三层职责划分、单一权威原则、规则层权威位置表）。
3. **状态追踪**：读对应小说的 `00_进度.md` 确认各文件成熟度（草稿/待校验/定稿），**只引用「定稿」数据**。
4. **通用模板只读**：`00_通用模板/` 下只放字段、规则与说明，**不得写入任何具体小说数据**。

> 其余执行规则（模板分类、单次加载模板 ≤4、`@引用` 与 ID 编号、跨章滑动窗口）一律见 `00_使用说明.md`，本文件不复述。

## 四、技能发现协议

技能分三层，不依赖特定 Agent 宿主：

1. **系统级** → `00_通用模板/03_任务技能/01_系统级/index.md`
2. **共用小说级** → `00_通用模板/03_任务技能/02_小说级/index.md`
3. **本书配置** → 该小说 `05_工作区/02_状态/02_技能/index.md` 与 `本书技能配置.md`

职责划分（技能/模板/规则/数据）见 `00_使用说明.md`「技能层」；规则优先级见 `00_系统架构规范.md` §十一。技能不得绕过 `00_使用说明.md` 的路由、引用、ID、状态与验收规则。

## 五、数据边界

- **正式小说数据**只存在于：`01_设定/`、`02_数据库/`、`03_规划/`、`05_工作区/02_状态/01_最新状态/`、`05_工作区/` 各级 `02_状态/`（履历与基线）、`10_正文/`。
- `05_工作区/` 下的提示词与模型输出是**生产过程数据**，不是正式小说数据，**不得作为后续任务的权威输入**。
- 工作区目录结构、冷读产物归位、落位门禁 → `00_使用说明.md`「动态工作区规则」与技能 `04_单章质量验收.md`。

## 六、工具索引

跨小说通用工具在 `02_工具/`。**用法与参数以各工具 `--help` 与对应技能文件为准，本表只做索引。**

| 工具 | 一句话用途 | 详见 |
|---|---|---|
| `progress_report.py` | 进度派生视图（可观测事实）＋ 与 `00_进度.md` 声明的成熟度对账 | 技能 `01_项目状态审计.md`「进度表对账」节 |
| `build_prompt.py` | 按六段骨架拼装自包含的云端提示词（单章细纲 / 正文），含前置门禁与内部标识自检 | 技能 `10_提示词拼装.md` |
| `audit_consistency.py` | 小说数据的确定性一致性审查（引用/索引/状态/伏笔/禁用词/工作区规范） | 技能 `01_项目状态审计.md`（含**规则代码目录**与局限说明） |
| `audit_rules.py` | **规则层自身**的一致性审查（死链/索引计数/技能索引/派生切片/权威位置表/重复规范正文/工具路径常量） | 同上技能「规则层审计」节；豁免登记在 `rules_audit.config.toml` |
| `check.sh` | 提交前一键三关：单元测试 + 规则层审计 + 各小说数据审计 | `--help` |
| `review_manuscript.py` | 正文/细纲的多模型独立对抗性冷读 | 技能 `04_单章质量验收.md` |
| `merge_chapter_state.py` | 章末把本章履历折叠进最新状态 | 技能 `03_章节状态对账.md` |
| `rebuild_global_state.py` | 改早期章节后从基线全量重折 | 技能 `06_章节回溯修改.md` |
| `build_state_snapshot.py` | 卷末快照 / 指定章开篇状态 / 刷新逐章 `00_开篇状态.md` | 技能 `03_章节状态对账.md`、`06_章节回溯修改.md` |
| `build_baseline.py` | 扫卡片生成基线候选（只读，不写基线） | 技能 `08_基线状态初始化.md` |
| `new_novel.py` | 脚手架新小说（算编号、拷骨架、建软链） | 技能 `07_新建小说.md` |
| `tts_chapter.py` | 定稿正文转音频存档，供作者听稿校对 | 技能 `09_章节配音.md` |
| `serve_audio.py` | 本地 HTTP 章节浏览/播放 + 播客 RSS | 技能 `09_章节配音.md` |
| `state_tree.py` / `_llm.py` / `llm.config.toml` | 状态共享库 / LLM 客户端（`backend = http｜opencode｜auto`，配置端点不可达时自动降级 opencode）/ 端点配置 | 文件内注释 |
| `99_一次性脚本_归档/` | 已完成的一次性迁移脚本，不再日常使用 | 该目录 `README.md` |

**状态模型**（唯一权威存储与「基线 ⊕ 履历折叠」公式）见 `00_系统架构规范.md` §十 与 `00_使用说明.md`「动态工作区规则」；**关系建模（W5）**见 `00_使用说明.md` ID 编号表与 `00_通用模板/03_字段词表.md` 第 6 节。

---

> 若将某本小说目录作为工作区根目录，以该小说目录中的 `AGENTS.md` 和 `00_通用模板/` 软链接为准。
