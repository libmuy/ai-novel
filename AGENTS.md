# 小说创作 Workspace 说明

> 本文档是 OpenCode 在本工作区的行为指南。**任务开始前，必须先读取 `00_通用模板/00_使用说明.md`**——路由表、@引用/ID 规范、Token 控制、一站式自检等核心规则全部集中在该文件，本文档仅作总览。

- **工作区根目录**：`4_小说`（即本仓库根），所有路径均相对此目录。
- **单本小说子文件夹**（如 `01_小说数据/00_苍玄/`）内的 `00_通用模板` 是指向根目录通用模板的**符号链接或目录联接**（`-> ../../00_通用模板`），只读，禁止写入任何具体小说数据。

## 一、工作区整体架构

本目录为小说创作专属 Workspace，整体采用「通用模板全局复用 + 单本小说数据隔离」的架构模式，彻底规避多本小说数据混淆问题。

### 1.1 核心目录构成

1. **小说数据目录**（`01_小说数据/`）：存放所有小说项目数据，单本小说独立一个子文件夹，实现数据完全隔离。
2. **通用模板目录**（`00_通用模板/`）：存储小说生成所需的统一数据格式模板、创作规则，为所有小说项目提供标准化通用能力，只读。

### 1.2 隔离复用机制

在单本小说专属子文件夹内创建指向通用模板的符号链接（已为 `00_苍玄` 建好：`01_小说数据/00_苍玄/00_通用模板 -> ../../00_通用模板`）。
效果：每本小说数据独立互不干扰，同时可直接复用统一的模板格式与创作规则。

小说数据目录统一以 `<NN>_<小说名>` 命名，`NN` 为从小到大自动递增的两位编号（从 `00` 起，新建小说取现有最大编号 +1，见 `00_通用模板/01_写作规则/06_新建小说清单.md`）。

## 二、小说初始创作工作流

小说创作风格设定、主题选择、整体章节规划等前期核心工作，统一使用云端大模型完成。

### 2.1 云端模型交互方式

通过浏览器端操作，以文本粘贴、附件上传两种形式向云端模型传输素材与需求，生成小说初始规划方案。

### 2.2 云端提示词生成机制

1. 云端模型所用的全部创作提示词，由 OpenCode 负责拼装；
2. 已通过 OpenCode 搭建专用拼装指令文件：`00_通用模板/04_提示词/00_云端提示词生成器.md`（固定骨架 + 12 项规划任务路由表，一份文件即一个自包含云端提示词，写入各小说 `05_工作区/00_提示词/` 存档）；
3. 工作逻辑：将小说初始规划的各类创作任务作为参数代入该指令文件，按路由表拼装出适配云端模型的标准提示词。

## 三、整体工作闭环总结

1. 底层架构：物理隔离（单本小说独立文件夹）+ 逻辑复用（软链接关联通用模板）；
2. 前期规划：云端模型负责风格、主题、章节框架生成；
3. 工具支撑：OpenCode 统一产出标准化提示词，适配云端模型调用，全程规范化、模板化完成小说初始创作工作。

## 四、任务执行约定

1. **必读入口**：每次任务开始，先读取 `00_通用模板/00_使用说明.md`（含任务路由表、引用语法、ID 编号规范、Token 控制原则）。
2. **架构规范**：首次执行任务前，读取 `00_通用模板/00_系统架构规范.md`，理解三层职责划分（设定层/规划层/创作层）、表现范式原则与软约束规则。
3. **状态追踪**：读取对应小说的 `00_进度.md` 确认各文件成熟度（草稿/待校验/定稿），只引用"定稿"数据。
4. **通用模板只读**：`00_通用模板/` 下文件仅定义字段、规则与说明，不得写入任何具体小说数据。
5. **模板分类**：`02_卡片模板/` = "要填的表"（定义字段，产生对应数据文件）；`01_写作规则/` = "要守的规矩"（行为约束，不产生数据文件）；`04_提示词/` = 提示词拼装指令。
6. **Token 控制**：单次任务加载模板不得超过 4 个，超出拆分为子任务；查表型数据（`01_修炼体系`、`04_资源`）按需取条，禁止整文件加载。
7. **引用与 ID**：填写数据一律使用 `@引用` 语法与统一 ID 编号（见 `00_使用说明.md`），不得用纯文本或模糊指代。
8. **跨章滑动窗口**：生成单章细纲与正文时，须遵循滑动窗口机制（单章细纲 `## 0. 上下文滑动窗口` 包含上章 300 字摘要与结尾最后 500 字原文），确保连续章节场景与语调过渡平滑。

## 五、项目技能发现协议

技能分为系统级、共用小说级和本书配置三层，不依赖特定 Agent 宿主：

1. **系统级技能**：读取 `00_通用模板/03_任务技能/01_系统级/index.md`，适用于整个小说生成系统。
2. **共用小说级技能**：读取 `00_通用模板/03_任务技能/02_小说级/index.md`，适用于单本小说的数据维护和写作流程。
3. **本书配置**：确定当前小说后，读取其 `05_工作区/02_状态/02_技能/index.md` 和 `05_工作区/02_状态/02_技能/本书技能配置.md`。

技能负责“如何执行任务”；模板负责字段；写作规则负责约束；小说数据负责内容。技能索引不是定稿数据，不参与正文设定引用。

**优先级**：安全、文件边界和数据完整性规则 > 本书配置 > 共用小说级技能 > 系统级流程。技能不得绕过 `00_通用模板/00_使用说明.md` 的路由、引用、ID、状态和验收规则。

## 六、项目骨架模板

新建小说时，从 `00_通用模板/05_项目骨架模板/` 复制目录骨架：

```bash
cp -r 00_通用模板/05_项目骨架模板/ 01_小说数据/<NN>_<小说名>/
```

骨架模板包含完整目录结构和初始化文件，确保新建小说符合标准结构。详见 `00_通用模板/01_写作规则/06_新建小说清单.md`。

## 七、运行归档说明

单本小说的 `05_工作区/` 记录任务执行的过程数据（非正式小说数据）：

- `05_工作区/00_提示词/` — 全书级提示词存档
- `05_工作区/01_模型输出/` — 全书级大模型原始输出
- `05_工作区/02_状态/` — 状态层：`00_基线状态/`（冻结基线）、`01_最新状态/`（唯一权威）、`02_技能/`、`03_状态对象白名单.md`、`04_描述合并缓存.jsonl`
- `05_工作区/XX_第XX部/XX_卷XX/XX_章XXXX/` — 递归工作区，包含提示词、模型输出与 `02_状态/`（如每章 `01_状态履历.md`、`00_开篇状态.md`）

正式小说数据只存在于 `01_设定/`、`02_数据库/`、`03_规划/`、`05_工作区/02_状态/01_最新状态/`、`05_工作区/`（各级 `02_状态/` 中的履历与基线）、`10_正文/`。

## 八、工具

跨小说通用的仓库维护/校验工具统一存放于 `02_工具/`（与 `00_通用模板/`、`01_小说数据/` 同级）。工具不属于某本书的数据，也不属于 `00_通用模板/` 的卡片模板/写作规则体系。

### 8.1 一致性审查脚本 `02_工具/01_小说通用工具/audit_consistency.py`

- **用途**：审查指定小说目录下的——顶层目录完整性（对照标准骨架 6 目录：`01_设定`/`02_数据库`/`03_规划`/`05_工作区/02_状态/01_最新状态`/`05_工作区`/`10_正文`）、`00_通用模板` 符号链接、`02_数据库/` 各分类总索引与实际文件一致性、`@类型.[TODO-xx]` 占位符回补状态、`01_状态履历.md` 字段合法性与「类型」列一致、`05_工作区/02_状态/01_最新状态/` 与冻结基线每对象文件树的合法性；**状态漂移** `state_drift`：从冻结基线折叠「已并入」的履历，比对 `05_工作区/02_状态/01_最新状态/` 存盘树，不一致即提示对象文件被手改或上游履历改动后未重折；**状态对象/字段注册**（W4.3）`state_object_unregistered`（error：状态里的对象名在对应数据库分类找不到同名卡片，且不在 `05_工作区/02_状态/03_状态对象白名单.md`）/ `state_field_undeclared`（error：状态字段未在该对象卡片「## 动态字段清单」声明）；**逐章开篇状态漂移**（W4.1）`chapter_opener_drift`（warning：`00_开篇状态.md` 与重算结果不符——派生文件被手改或上游履历改动后漏跑 `--write-chapter-openers`）；**卷大纲章节归属单一权威**（`plan_beat`，见 `00_系统架构规范.md` §二·A，均 warning）：`PLAN_BEAT001` 其它区块把「章节」当字段记（带章节列 / 【出场对象ID清单】）；`PLAN_BEAT002` 伏笔列表某 FH 的埋设/回收章 N，第 N 章节拍表摘要未 `@引用` 该 FH；`PLAN_BEAT003` 其它区块声明「第N章 × 某对象」但第 N 章摘要未 `@引用`（或节拍表无第 N 章行）；`PLAN_BEAT004` 【退场配角】的角色，其退场未落到某章节拍表摘要（含退场/死亡字眼）；`PLAN_BEAT005` 寿元/续命倒计时类配角，本卷【退场配角】或【卷末状态】未交代其存殁；`PLAN_BEAT006` 【关系变化】弧线涉及的对象在节拍表摘要里追溯不到；**工作区架构规范**（`workspace`）：校验 00-based 连续编号、唯一性、提示词/模型输出同名配对及禁止残留旧目录（`00_全局`/`历史回填`/`本级工作资料` 等）。
- **用法**：`python3 02_工具/01_小说通用工具/audit_consistency.py <小说目录路径> --format json`。默认 json 输出，供 Agent 直接解析 `issues` 数组、按 `suggested_action` 执行修改；`--format text` 为人类可读模式。
- **使用时机**：建议在每次云端回填分割完成、或占位符回补前后各跑一次；`rebuild_global_state.py` 重折后**必须**跑一次。
- **局限说明**：脚本对 `00_进度.md` 中各分类的权威产出路径（如 `02_数据库/02_地理区域/`）存在硬编码耦合，用于定位「源分类成熟度」表行；若数据库分类目录调整，需同步更新脚本内 `CATEGORY_KEYWORD_IN_PROGRESS`（与 `STANDARD_TOP_DIRS`）。

### 8.2 状态脚本

状态模型：`05_工作区/02_状态/01_最新状态/`（每对象一文件、按类目分层）是全书唯一权威状态存储；
`05_工作区/02_状态/01_最新状态/` == 冻结基线 `05_工作区/02_状态/00_基线状态/` ⊕ 全部 `01_状态履历.md`
的折叠。描述类字段的 LLM 合并结果存进独立的 append-only 缓存
`05_工作区/02_状态/04_描述合并缓存.jsonl`（派生数据，按内容指纹做键，禁止手工编辑）；
`01_状态履历.md` 永不被脚本改写，永远是人工原文。共享逻辑在 `02_工具/00_系统级/state_tree.py`，
LLM 客户端在 `02_工具/00_系统级/_llm.py`。

| 脚本 / 文件 | 用途 |
|---|---|
| `merge_chapter_state.py` | 章末：`--chapter-dir <本章>` 把本章履历折叠进 `05_工作区/02_状态/01_最新状态/`（始终从基线全量重折）。描述类字段有实质变更时调**一次** LLM 合并旧+新文本，合并结果追加进独立的 append-only 缓存 `04_描述合并缓存.jsonl`（键含旧/新值指纹）；**本章履历原文永不被改写**。LLM 不可用且有描述变更 → 退出码 2、不写文件；纯运算章节无需 key。`--dry-run` 预览、`--backup`/`--no-backup`（默认备份）、`--no-llm`、`--force`。运算-数值非法值仍中止退出码 2；所有写盘原子。 |
| `rebuild_global_state.py` | 改早期章节后：`<小说目录>` 从基线全量重折覆盖 `05_工作区/02_状态/01_最新状态/`。**必须先 `--dry-run`**。含描述变更未命中合并缓存的章（首次合并，或上游章节被改动导致前提变化）需 `--merge-pending`。见技能 `03_任务技能/02_小说级/06_章节回溯修改.md`。 |
| `build_state_snapshot.py` | 只读、不调 LLM。`--volume-dir` → 卷末快照 `99_卷末状态快照.md`；`--at-chapter` → 该章开篇时的全量状态（回溯改旧章时的滑动窗口输入）；`--write-chapter-openers <小说目录>` → 为每章生成/刷新 `00_开篇状态.md`（值拷贝派生视图，按各章单章细纲「## 出场对象」小节裁剪；关系对象按「至少一端在场」拉取；清单缺失写全量并告警）。`merge_chapter_state.py` / `rebuild_global_state.py` 写完最新状态后会自动调用后者。 |

> **关系建模（W5）**：对称关系（敌对/结盟/血亲/同门/竞争…）升为一等对象 `关系.<甲>&<乙>`（ASCII `&`、两端 Unicode 排序、`06_关系/` 类目）；非对称关系记从属方字段（`物品.Y 的「持有者」`、`角色.X 的「师承」`、`势力.X 的「隶属」`），反向由 `merge_chapter_state.py`/`rebuild_global_state.py` 写出的派生视图 `05_工作区/02_状态/01_最新状态/00_持有物品反查.md` 提供（只读、非权威）。已废除 `持有物品` / `关键关系` / `主要关系` 字段。字段定义见 `00_通用模板/03_字段词表.md` 第 6 节。
| `build_baseline.py` | 只读卡片、不调 LLM、**不写基线**。`<小说目录>` 扫全部人物/势力卡的「## 动态字段清单」已填初值 → 生成**基线候选**树到 `05_工作区/02_状态/00_基线候选/`，并报告哪些字段初值待用户确认、哪些卡缺清单。Agent 按技能 `08_基线状态初始化.md`【基线范围规则】筛选后提升为 `00_基线状态/`。`--no-write` 只出报告，`--format json`。 |
| `migrate_state_layout.py` | 一次性：把旧的扁平 `05_工作区/02_状态/01_最新状态/0N_*状态.md` + 逐章 `03` 迁移为每对象一文件的树 + 建冻结基线。只改结构不改内容。 |
| `new_novel.py` | `<小说名>` 脚手架新小说（算编号、拷骨架、建软链、填占位符）。无 LLM。见技能 `07_新建小说.md`。 |
| `state_tree.py` / `_llm.py` / `llm.config.toml` | 共享库 / LLM 客户端（OpenAI 兼容 `/chat/completions`）/ 端点配置。当前指向本地 llama.cpp（`http://ai-station.local:8080/v1`，`api_key_required = false`，无密钥）；需鉴权的端点走环境变量或 gitignore 的 `llm.secret.toml`。 |

> 若将某本小说目录作为工作区根目录，以该小说目录中的 `AGENTS.md` 和 `00_通用模板/` 软链接为准。