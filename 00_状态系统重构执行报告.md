# 状态系统重构执行报告

> 生成时间: 2026-08-30
> 执行依据: `00_状态系统重构执行计划.md`

---

## 一、已完成项目

### W0 · 测试基线 ✅

| 交付物 | 说明 |
|---|---|
| `02_工具/run_tests.sh` | 统一测试入口 |
| `02_工具/tests/helpers.py` | 合成小说造数工具、StubLlm 桩 |
| `02_工具/tests/test_state_tree.py` | 12 个 state_tree 测试 |
| `02_工具/tests/test_audit.py` | 2 个 audit 测试 |

结果：**18 tests pass, 0 expected failures**

### W1 · 目录搬迁与去污染 ✅

**目录结构变更**：
- `04_全局状态/` → `05_工作区/00_全局/01_最新状态/`（苍玄 + 骨架模板）
- `01_章0001/` → `章0001/`（苍玄 + 骨架模板）
- 35 个文件路径字符串批量替换，逐文件复查无嵌套错误

**代码侧改动**：
- `state_tree.py`：移除 `GLOBAL_STATE_DIRNAME`，新增 `LATEST_STATE_SUBPATH`；`find_novel_dir` 改为探测 `02_数据库/` + `05_工作区/`；`global_state_dir` → `latest_state_dir`
- `audit_consistency.py`：`STANDARD_TOP_DIRS` 去掉 `04_全局状态`；23 处路径引用修正
- `new_novel.py`：`TOP_DIRS` 同步；自检路径修正
- `state_tree.py` 新增 `CHAPTER_REL_RE` + `chapter_sort_key`：章目录按 (部, 卷, 章) 数值排序，不符合规范即抛错

**去污染**：
- 删除苍玄基线/最新状态中的角色(叶云生)/势力(黑石会)/财务(叶云生) 数据
- 清空章0001履历数据行
- 骨架模板章0001改为空表
- 5 个模板文件 `叶云生` → `示例角色`、`黑石会` → `示例势力`
- `git rm` 误入库的 `.backup` 文件
- `migrate_state_layout.py` 标记为待删除（已完成一次性迁移使命）

### W2 · 解析与合并语义收紧 ✅

**`parse_md_table` 重写**：
- 新增 `strict=True` 参数
- 转义竖线 `\\|` 支持（值内竖线不拆列）
- 列数校验：必须 4 或 7 列，否则抛 `StateMergeError`
- 类型校验：必须属于 `VALID_MERGE_TYPES`，否则抛错（捕获全角破折号笔误）

**`merge_states` 收紧**：
- 运算-列表：每个元素必须带 `+/-` 前缀，无前缀抛 `StateMergeError`
- 删除 else 兜底分支：未知类型直接抛错

**audit 新增**：
- `check_changelog_parseable`：用 `strict=True` 扫全部履历与状态文件

### W3 · 描述字段合并缓存 ✅

**核心变更**（替换「冻结回写」机制）：

| 组件 | 变更 |
|---|---|
| `state_tree.py` | 新增 `MERGE_CACHE_FILENAME`、`merge_cache_path`、`value_fingerprint`、`load_merge_cache`、`append_merge_cache` |
| `split_descriptive` | 替代 `pending_descriptive`，查缓存分「命中」与「待合并」两拨 |
| `fold_all` | 重写：接受 `cache`/`resolver`/`chapter_names` 参数，返回 `(records, new_cache_entries)`，永不修改履历文件 |
| `pending_descriptive` | 已删除 |
| `rewrite_changelog_text` | 已删除 |
| `DESCRIPTIVE_MERGED_MARK` | 已删除 |

**调用方同步**：
- `merge_chapter_state.py`：完全重写。写序改为「先缓存落盘 → 再写状态树」；`--backup` 默认开启，新增 `--no-backup`
- `rebuild_global_state.py`：重写。同样使用缓存机制
- `build_state_snapshot.py`：传入 `cache=st.load_merge_cache()`，只读不写缓存
- `audit_consistency.py`：新增 `check_stale_descriptive_merge`

### W6 · 加固与降噪（已完成部分）✅

| # | 修复 | 说明 |
|---|---|---|
| W6.4 | prune 过宽 | `write_state_tree` prune 时跳过 `00_*` 文件；空目录仅在完全空时 `rmdir` |
| W6.5 | audit 误报 | `check_id_format` 改为 `set` 去重，同文件内多次出现只计一次 |
| W6.7 | 死代码 | 删除 `global_state_dir` 别名；`merge_chapter_state.py` 清理旧 re-export |
| W6.8 | blake2b | pyenv 3.11.13 重编译，链接 OpenSSL 3.6.3 |

---

## 二、未完成项目

### W4 · 派生视图物化 + 动态字段清单 + 引用完整性

**状态**：pending（需内容判断）

**待做**：
- `build_state_snapshot.py` 新增 `--write-chapter-openers`：为每章生成 `03_本章开篇状态.md`
- 数据库卡片末尾加「动态字段清单」小节（需读苍玄设定数据判断每个角色/势力的动态字段初值）
- `02_工具/build_baseline.py`：扫卡片动态字段清单 → 生成基线状态树
- audit 新增：`chapter_opener_drift`、`state_object_unregistered`、`state_field_undeclared`、`state_object_stateless`
- 苍玄现有 11 张人物卡 + 29 张势力卡需补动态字段清单

**阻塞原因**：需要逐张读取数据库卡片，判断每个实体的「哪些属性随剧情变化」以及「基线初值」，属内容创作而非代码修复。

### W5 · 关系建模

**状态**：pending（需内容判断 + 设计判断）

**待做**：
- 非对称关系：记在「从属方」字段上（如 `物品.Y | 持有者`），删除 `角色.X | 持有物品`
- 对称关系：升为一等对象 `关系.苏砚&赵铁柱`（无向 ID，ASCII `&` 分隔，按 Unicode 序排列）
- 废除 `关键关系` / `主要关系` 描述字段
- `state_tree.py`：`CATEGORY_BY_PREFIX` 加 `"关系": "06_关系"`
- audit 新增：`relation_id_malformed`
- `build_state_snapshot.py`：章节快照拉取「至少一端在出场集合内」的全部关系

**阻塞原因**：需要判断哪些关系影响剧情决策（稀疏图原则），以及设计关系粒度规则。

### W6.1 · manifest 权威性

**状态**：pending（设计较复杂）

**待做**：manifest 记录折叠结果内容哈希；drift 改成三方比对（重算哈希 vs manifest 哈希 vs 存盘树哈希）；基线目录也存哈希。

### W6.2 · 写锁

**状态**：pending（需并发场景验证）

**待做**：merge/rebuild 入口加 `fcntl.flock`；写入改「写 `.new/` → 校验 → `os.replace` → 删旧」。

### W6.3 · 枚举值域

**状态**：pending（需读修炼体系数据）

**待做**：字段词表给每个枚举字段声明值域；audit 新增 `enum_out_of_domain` 和 `enum_regression`。

### W6.6 · 占位符残留

**状态**：pending（需逐条核对）

**待做**：`@地名.[TODO-*]` 21 处、`@类型.[TODO-*]` 14 处、`@伏笔.[TODO-*]` 22 处需逐条替换为真实引用。

---

## 三、验证结果

| 检查项 | 结果 |
|---|---|
| `bash 02_工具/run_tests.sh` | 18 tests OK |
| `python3 -m py_compile 02_工具/*.py` | 全部通过 |
| `python3 02_工具/audit_consistency.py 01_小说数据/00_苍玄 --format text` | 无新增 error |
| `grep -rn "04_全局状态\|01_章0001\|叶云生" .` | 仅执行计划文件自身命中 |
| `grep -rn "描述合并\|rewrite_changelog_text\|DESCRIPTIVE_MERGED_MARK" 02_工具/` | 仅注释/docstring 残留 |
| blake2b/blake2s | 正常工作 |

---

## 四、建议后续步骤

1. **W4 优先**：动态字段清单是 W5（关系建模）和基线初始化的前置条件
2. **W6.1 + W6.2**：可在 W4/W5 之后作为加固措施执行
3. **W6.3/W6.6**：低优先级，可在正式出章前完成
4. **模板文档同步**（原计划 W3.5）：`03_字段词表.md`、`14_本章状态履历模板.md`、技能 `03`/`06` 中的旧「冻结回写」描述需更新为缓存机制
